apiVersion: v1
kind: Service
metadata:
  name: orders-api
  namespace: orders
  labels: { app: orders-api }
spec:
  type: ClusterIP
  selector: { app: orders-api }
  ports:
    - name: http
      port: 80
      targetPort: http
